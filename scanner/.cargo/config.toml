[build]
target = "wasm32-unknown-unknown"

[target.wasm32-unknown-unknown]
# Deterministic codegen: single codegen unit eliminates nondeterministic
# partitioning that would otherwise vary across host environments.
codegen-units = 1
opt-level = "z"
lto = true
strip = true
